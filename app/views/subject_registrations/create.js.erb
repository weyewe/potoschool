$(document).ready(function(){

  var $checkbox = $("#provider_<%=  @subject_id %>-consumer_<%=  @user_id %>");
  var $row = $checkbox.parents("tr");
  var $td_membership = $(".total-subjects-taken", $row );
  $td_membership.text('<%= @student.subject_registrations.count %>');


});