$(document).ready(function(){

  <% new_row = escape_javascript(  render :partial => "subject_registrations/subject_registration_entry", :locals => {
            :subject => @subject,
            :student => @student 
          }) %>
          
  $("#subject_registration_entry-<%= @student.id %>").replaceWith('<%= new_row %>');
});