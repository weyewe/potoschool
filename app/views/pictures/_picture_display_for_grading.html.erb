
<li class="span3">
  <div class="thumbnail">
    
    <%# if not project_submission.approved? %>
      <%#= link_to  grade_project_submission_picture_path( original_picture),
       :class => "thumbnail" , :style => "height:160px; overflow:hidden;" do %>
        <%#= image_tag original_picture.index_image_url %>
      <%# end %>
    <%# else %>
      <%#= link_to  grade_project_submission_picture_path( project_submission.approved_picture),
       :class => "thumbnail" , :style => "height:160px; overflow:hidden;" do %>
        <%#= image_tag project_submission.approved_picture.index_image_url %>
      <%# end %>
    <%# end %>
    
    <!--  in all cases, show the latest revision  -->
    <!-- <%# if not original_picture.any_picture_submission_approved? %>
         <%#= link_to  grade_project_submission_picture_path( original_picture.last_revision),
          :class => "thumbnail" , :style => "height:160px; overflow:hidden;" do %>
           <%#= image_tag original_picture.last_revision.get_icon("index") %>
         <%# end %>
       <%# else %>
         <%#= link_to  grade_project_submission_picture_path( original_picture.approved_picture),
          :class => "thumbnail" , :style => "height:160px; overflow:hidden;" do %>
         
           <%#= image_tag original_picture.approved_picture.get_icon("index") %>
         <%# end %>
       <%# end %> -->
    
    
    <%= link_to  grade_project_submission_picture_path( original_picture.last_revision),
     :class => "thumbnail" , :style => "height:160px; overflow:hidden;" do %>
      <%#= image_tag original_picture.last_revision.index_image_url %>
      <%= image_tag original_picture.last_revision.get_icon("index") %>
    <% end %>
  
    
    
    <!-- <img alt="" src="http://placehold.it/260x180"> -->
    <div class="caption">
      <table cellpadding="0" cellspacing="0" border="0" 
            class="table table-striped table-bordered" id="example">
        
        <tbody>
          <tr>
            <td colspan="2" style="overflow:hidden"><%= original_picture.name[0..20] %></td>
          </tr>
          
          <tr>
            <td>Revisions</td>
            <td><%= original_picture.revisions.where(:is_deleted => false).count %></td>
          </tr>
          
          <tr>
            <td>Past Approval?</td>
            <td><%= original_picture.revisions.where(:is_approved => true ).count %></td>
          </tr>
          
          <% last_revision =  original_picture.last_revision %>
          <tr>
            <td> Approval</td>
            <td><%= get_picture_status_label(last_revision).html_safe %></td>
          </tr>
          
          <tr>
            <td> Score</td>
            <td><%= last_revision.score  %></td>
          </tr>
          
         </tbody>
      </table>
     
    </div>
     
     
        
      
  </div>
</li>

