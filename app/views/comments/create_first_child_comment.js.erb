/*
What should we do? 

This is the first child!!! 

OK OK OK , calm down.. First, we should remove the form for the first child

Then, we should add the <ol> for the children
*/

$(document).ready(function(){
  $("#root_comment-<%= @root_comment.id %>").remove(); 
  
  
  
  
});